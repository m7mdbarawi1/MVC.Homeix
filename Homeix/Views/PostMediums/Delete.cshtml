@model Homeix.Models.PostMedium

@{
    ViewData["Title"] = "Delete Post Media";
}

<div class="container-fluid mt-4">

    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12 text-center">
            <h1 class="text-danger">Delete Post Media</h1>
            <p class="text-muted">Are you sure you want to delete this media item?</p>
        </div>
    </div>

    <!-- Centered Card -->
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-7">
            <div class="card shadow-sm border-danger">

                <!-- Card Header -->
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Confirm Deletion</h5>
                </div>

                <!-- Card Body -->
                <div class="card-body">
                    <dl class="row">

                        <dt class="col-sm-4 fw-bold">Post Type</dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.PostType)
                        </dd>

                        <dt class="col-sm-4 fw-bold">Post ID</dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.PostId)
                        </dd>

                        <dt class="col-sm-4 fw-bold">Media Path</dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.MediaPath)
                        </dd>

                        <dt class="col-sm-4 fw-bold">Uploaded At</dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.UploadedAt)
                        </dd>

                    </dl>
                </div>

                <!-- Card Footer -->
                <div class="card-footer d-flex justify-content-end gap-2">
                    <form asp-action="Delete" method="post" class="d-inline">
                        <input type="hidden" asp-for="MediaId" />
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </form>

                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left-circle"></i> Cancel
                    </a>
                </div>

            </div>
        </div>
    </div>
</div>
