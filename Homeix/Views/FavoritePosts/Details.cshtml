@model Homeix.Models.FavoritePost

@{
    ViewData["Title"] = "Favorite Post Details";
}

<div class="container-fluid mt-4">

    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12 text-center">
            <h1 class="fw-bold">Favorite Post Details</h1>
        </div>
    </div>

    <!-- Centered Card -->
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">

            <div class="card shadow-sm">

                <!-- Card Header -->
                <div class="card-header">
                    <h5 class="mb-0">Details</h5>
                </div>

                <!-- Card Body -->
                <div class="card-body">
                    <dl class="row mb-0">

                        <dt class="col-sm-4 fw-bold">
                            @Html.DisplayNameFor(model => model.PostType)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.PostType)
                        </dd>

                        <dt class="col-sm-4 fw-bold">
                            @Html.DisplayNameFor(model => model.PostId)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.PostId)
                        </dd>

                        <dt class="col-sm-4 fw-bold">
                            @Html.DisplayNameFor(model => model.AddedAt)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.AddedAt)
                        </dd>

                        <dt class="col-sm-4 fw-bold">
                            @Html.DisplayNameFor(model => model.User)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.User.UserId)
                        </dd>

                    </dl>
                </div>

                <!-- Card Footer -->
                <div class="card-footer d-flex justify-content-end gap-2">
                    <a asp-action="Edit" asp-route-id="@Model?.FavoritePostId" class="btn btn-secondary">
                        Edit
                    </a>
                    <a asp-action="Index" class="btn btn-primary">
                        Back to List
                    </a>
                </div>

            </div>

        </div>
    </div>
</div>
